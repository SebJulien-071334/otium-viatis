# Copier ce fichier en .env.local et remplir les valeurs
# Ne JAMAIS committer .env.local

# Clé API Navitia — côté serveur uniquement (Vercel Function)
NAVITIA_API_KEY=your_navitia_token_here

# URL flux CSV temps réel TAM (optionnel si non configuré)
# TAM_CSV_URL=https://data.montpellier3m.fr/...
